# Core API
fastapi
uvicorn[standard]

# AI

openai

# Database (PostgreSQL)
sqlalchemy>=2.0
psycopg2-binary

# Migrations
alembic

# Environment variables
python-dotenv

# Validation (already included in FastAPI but explicit is fine)
pydantic
pydantic[email]
pydantic-settings
email-validator

# Auth / Security
python-jose[cryptography]
passlib[argon2]>=1.7.4

# Password hashing backend
argon2-cffi>=21.3.0

# Async / performance (optional but useful for your game)
redis
asyncpg

# CORS (frontend connection)
fastapi[all]

# Testing (optional but recommended)
pytest
httpx